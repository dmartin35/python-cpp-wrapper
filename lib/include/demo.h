#include <time.h>

#ifndef DEMO_H_   /* Include guard */
#define DEMO_H_

/* functions declaration */
char * echo(char * name);
struct tm * now(void);
long addition(long a, long b);

#endif // DEMO_H_

